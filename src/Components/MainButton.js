import React from "react";

const MainButton = () => {
  return (
    <div className="sm:text-xs md:text-md">
      <button className="bg-black p-3 text-white   rounded-lg hover:bg-gray-800 hover:shadow-lg hover:text-gray-200">
        Join the waitlist
      </button>
    </div>
  );
};

export default MainButton;
